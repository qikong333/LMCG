import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { EditeAddressPage } from './edite-address';

@NgModule({
  declarations: [
    EditeAddressPage,
  ],
  imports: [
    IonicPageModule.forChild(EditeAddressPage),
  ],
})
export class EditeAddressPageModule {}
